# Domain docs

This is a single-context repo.

Before work in a domain area, read the root `CONTEXT.md` and relevant decisions in
`DECISIONS.md`. Use the glossary’s terms in issues, specs, tests, and code
discussions. If a proposal conflicts with an ADR, call out the conflict.
If a term is missing, resolve it through domain modeling when it matters.
